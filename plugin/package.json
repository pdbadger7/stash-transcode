{
  "name": "stash-external-transcode-plugin",
  "version": "0.1.0",
  "description": "Stash UI plugin for external transcoding",
  "scripts": {
    "build": "echo 'Plugin is distributed as YAML + TypeScript source'",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "eslint src --ext .tsx,.ts"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "eslint": "^8.55.0",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  }
}
